#Server port
server:
  port: 8888

#Git repo location.
spring:
  cloud:
    config:
      server:
        git:
          force-pull: true
          uri: https://github.com/Komal148/config-server-repo.git

#  data:
#     mongodb:
#        database: sunidhi
#        port: 27017
#        host: localhost
#
##Disable security of the Management endpoint
management:
  security:
      enabled: false

